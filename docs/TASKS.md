# LiveListen — Task Queue

This is the queue the **daily routine** reads. It takes the **top unblocked**
unchecked task, implements it on a `routine/<slug>` branch, and opens a PR.

Format: `- [ ] <title> — <acceptance criteria>`
- Top = highest priority. Add new work at the top to prioritize it.
- A task with a `> blocked: <reason>` line underneath is skipped until cleared.
- The routine ticks `[x]` inside its PR when done.

## Queue

- [ ] Add `aria-label="Search performances"` to the search `<input>` in `src/app/search/page.tsx` — the input only has a placeholder (screen readers don't reliably announce placeholders as labels); add `aria-label`, extend `src/app/search/page.test.tsx` to assert the attribute is present on the input element; no visual change.
- [ ] Add test coverage for `src/app/artist/[name]/error.tsx` — the artist error boundary exists (alongside `not-found.tsx` and `loading.tsx`) but has no test file; add `src/app/artist/[name]/error.test.tsx` following the `src/app/error.test.ts` pattern; cover: renders without throwing; includes a reset mechanism (button or link); heading text is present.
- [ ] Add `openGraph.images` to artist `generateMetadata` in `src/app/artist/[name]/layout.tsx` — the layout sets `title` and `alternates.canonical` but lacks `openGraph.images`, so social link previews have no image; add a static default OG image path (e.g. `[{ url: '/og-default.png' }]`) and extend `layout.test.tsx` to assert `metadata.openGraph?.images` is truthy.
- [ ] Add `src/app/artist/[name]/not-found.test.tsx` — the `not-found.tsx` for the artist route has no test file; add one following `src/app/not-found.test.tsx`'s pattern; cover: renders without throwing; includes a link back to the discover/landing page; artist-specific not-found text is present.
- [ ] Add tests for `src/app/artist/[name]/page.tsx` — the most-visited data-rich page has zero test coverage; mock `@/lib/supabase`, `next/navigation`, `@/components/PageNav`, `@/components/SuggestFooter`, and `@/components/SaveButton`; cover: artist name and performance count render when data loads; error state shows when Supabase throws; not-found triggers when `performances` is empty; at least one `PerfCard` renders with the expected aria-label.
- [ ] Add keyboard accessibility to `EventCard` in `src/app/landing/page.tsx` — `EventCard` has an `onClick` handler but no `role="link"`, `tabIndex={0}`, or `onKeyDown` (Enter/Space), making it unreachable for keyboard-only users; add the same three attributes following the pattern shipped in #40 for `ArtistCard` and `VenueCard`; add a test asserting the element is keyboard-focusable and Enter fires the router push.
- [ ] Add keyboard accessibility to `PerfCard` in `src/app/artist/[name]/page.tsx` — `PerfCard` (line ~64) wraps an `onClick` with no `role`, `tabIndex`, or `onKeyDown`; apply `role="link"`, `tabIndex={0}`, `onKeyDown` (Enter/Space → router push) following the pattern from #40; add a test covering keyboard navigation.
- [ ] Add `aria-live="polite"` to results-count badges on `/artists` and `/venues` — the `N ARTISTS` and `N VENUES` count spans update silently when the filter changes; add `aria-live="polite"` `aria-atomic="true"` to each span so screen-reader users hear the updated count after filtering; no visual change; add tests asserting the attribute is present on each badge.
- [ ] Add `/login` to the `disallow` list in `src/app/robots.ts` — `/login` is publicly reachable but provides no search-engine value; add it alongside `/profile`, `/developer`, and `/api` in the existing `disallow` array; if a `src/app/robots.test.ts` exists, extend it; otherwise add a minimal test asserting `/login` appears in the disallowed set.
- [ ] Add `Permissions-Policy` header to `next.config.ts` — the `headers()` config ships CSP and the three hardening headers from PR #67 but no `Permissions-Policy`; add `Permissions-Policy: camera=(), microphone=(), geolocation=()` to lock down unneeded browser APIs for every route; no new dependencies; `npm run typecheck` and `npm run build` must pass.
- [ ] Add `loading.tsx` for `/landing` route — `src/app/landing/` has only `layout.tsx` and `page.tsx` with no loading skeleton; add a minimal `loading.tsx` Server Component (matching the `#030014` dark background, centred pulse placeholder) so the route streams a frame before client hydration, consistent with `/search`, `/artists`, `/venues`, and `/profile` which already have `loading.tsx` (shipped in #22).
- [ ] Improve `/landing` page title — `src/app/landing/layout.tsx` exports `metadata: { title: "Home" }` which renders as a bare `"Home"` browser-tab title with no brand context; replace with a descriptive title such as `"LiveListen — Discover Live Performances"`; update or add a `layout.test.tsx` assertion that the exported `metadata.title` is not the single word `"Home"`.
- [ ] Extract TheAudioDB fetch into a typed `src/lib/audioDb.ts` client module — `src/app/api/artists/search/route.ts` constructs the TheAudioDB URL inline and casts the response as `any`; extract to `src/lib/audioDb.ts` exporting `searchArtists(query: string, signal?: AbortSignal): Promise<AudioDbArtist[]>` with a typed `AudioDbArtist` interface matching the fields actually used (`strArtist`, `strArtistThumb`); add `src/lib/audioDb.test.ts` covering the typed response shape, the empty-array fallback when `artists` is null, and the 5-second timeout; simplify `route.ts` to call the helper.
- [ ] Add keyboard focus trap for the account menu in `PageNav.tsx` — the dropdown menu opens on avatar click but does not trap focus: pressing Tab moves focus out of the menu to background elements behind the `position:fixed` overlay, which is an accessibility failure for keyboard-only users; add `onKeyDown` handling on the menu container so Escape closes the menu and Tab cycles only within the menu items (Profile, Sign out); cover these interactions in `PageNav.test.tsx`.
- [ ] Add `Cache-Control` headers for static performance/artist data — `src/app/artist/[name]/page.tsx` and `src/app/search/page.tsx` refetch the same Supabase rows on every navigation with no caching layer; add Next.js `revalidate`/`fetch` cache hints (or `unstable_cache`) so repeat visits within a short TTL skip the round-trip.
  > blocked: `src/app/artist/[name]/page.tsx` and `src/app/search/page.tsx` are both `'use client'` components that fetch directly via the browser-side Supabase JS client (`@/lib/supabase`), not via Next.js `fetch()` or a server component — so `revalidate`/`fetch` cache hints and `unstable_cache` (all server-only mechanisms) don't apply to this data path at all. A real fix means restructuring these pages onto a server-side data layer first (also flagged as a roadmap item below), which is an architectural change, not a "smallest change" — needs a scoped follow-up task once that restructuring is designed.
- [x] Add a `robots.test.ts`-style test for `src/app/sitemap.ts`'s `lastModified` field — resolved by the `lastModified` task below.
- [ ] Migrate `src/app/search/page.tsx`'s Supabase fetch to a server-side data layer — per `docs/ARCHITECTURE.md`'s convention ("Keep DB access in server code / `src/lib/**`, not client components"), this page is `'use client'` and calls `@/lib/supabase` (the browser client) directly, same as `artist/[name]`, `artists`, and `venues`. Pick `/search` first (already the most-visited route and the one with the most acceptance-criteria payoff): move the initial query into a server component or route handler, keep the existing debounce/AbortController logic for the client-side re-search-on-keystroke behavior, and leave `artists`/`venues`/`artist/[name]` as separate follow-up tasks once this pattern is proven. Acceptance: no `@/lib/supabase` import remains in the client bundle for the initial page load; existing `page.test.tsx` still passes (adjusted for the new data-fetching boundary).
  > blocked: requires the architect agent to design the server-component boundary first — specifically where the RSC/client split goes when the page has both an initial server fetch and a client-side re-search-on-keystroke loop; the "smallest change" mandate can't safely be applied without a concrete design for the data-fetching boundary and how `page.test.tsx` is adjusted. Needs an architect-authored design task before implementation.
- [x] Add a test suite for `src/components/PageNav.tsx` — currently has zero coverage despite holding auth-menu and active-tab logic; mock `useUser`/`useSignOut`, cover: active tab highlighting per route (including the `/artist/*` → "Artists" and root/`landing` → "Discover" fallthroughs), account menu open/close on avatar click, "Sign out" calls `useSignOut`, and unauthenticated state renders "Sign up free" routing to `/login`.
- [x] Add tests for `src/app/profile/page.tsx` — mock `@/lib/useUser`, `@/lib/supabase`, and `@/lib/savedStore`; cover: (a) loading spinner while `userLoading=true`; (b) empty-saved state shows "Browse Performances" CTA; (c) saved grid renders a card per saved entry; (d) remove button calls `toggleSaved` with the correct ID; (e) Supabase load error shows error state with a retry button; (f) stats tab renders platform breakdown when saved list is non-empty; (g) optimistic remove rolls back the list on a `toggleSaved` rejection.
- [x] Fix silent Supabase error in `src/app/venues/page.tsx` — the page silently discards a failed load (`if (!data) { setLoading(false); return }`) with no user feedback, unlike `src/app/artists/page.tsx` which has a `loadError` boolean state and a visible error banner; add the same `loadError` / catch pattern and a corresponding test case in `src/app/venues/page.test.tsx`.
- [x] Migrate `<img>` to `next/image` in `src/app/artist/[name]/page.tsx` — two raw `<img>` tags to migrate: the performance thumbnail in `PerfCard` (line 69) and the artist avatar circle (line 204); the fanart error-detection probe (line 187, `display:none`, `alt=""`) is a side-effect element and may remain a raw `<img>`; add `www.theaudiodb.com` to `next.config.ts` `remotePatterns` (artist thumbs and fanart originate there); use `fill` for thumbnails and `width={110}` `height={110}` for the avatar; keep `onError` fallback chains.
- [x] Add `X-Content-Type-Options`, `X-Frame-Options`, and `Referrer-Policy` headers to `next.config.ts` — three standard hardening headers currently absent from `headers()`; add `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, and `Referrer-Policy: strict-origin-when-cross-origin` to the existing `/(.*)`route config alongside the CSP header; no new dependencies; `npm run typecheck` and `npm run build` must stay green.
- [x] Add `aria-label` to filter inputs on `/artists` and `/venues` — `src/app/artists/page.tsx` line 184 and the equivalent filter `<input>` in `src/app/venues/page.tsx` both lack an accessible label; add `aria-label="Filter artists by name"` and `aria-label="Filter venues by name"` respectively; extend the existing `artists/page.test.tsx` and `venues/page.test.tsx` to assert the label attribute is present on each input; no visual change.
- [x] Add `lastModified` to `src/app/sitemap.ts` entries and a test — static entries should carry `lastModified: new Date()` (build-time stamp) and dynamic artist entries should derive `lastModified` from the most-recent `performance_date` in the Supabase row (falling back to `new Date()` when the column is null); add a test in `src/app/sitemap.test.ts` asserting `lastModified` is a valid `Date` instance on both static and dynamic entries; this also resolves the blocked task above.
- [x] Add tests for `src/app/landing/page.tsx` — no test file exists; mock `next/image`, `next/link`, and `next/navigation` following `src/components/SaveButton.test.tsx`'s pattern; cover: page renders without throwing; primary hero heading is present in the output; the main CTA button/link targets `/login`; at least one image element renders in the "Recent Drops" section.
- [x] Extract `computeStats` and `mode` from `src/app/profile/page.tsx` into `src/lib/computeStats.ts` — both are pure functions embedded in the 473-line page file with no unit tests; move them to a standalone module and add `src/lib/computeStats.test.ts` covering: empty saved list returns zero-value stat badges; `mode()` picks the most-frequent value and returns `null` when all values are tied or the list is empty; platform breakdown percentages are rounded correctly; `duration_minutes: null` entries contribute 0 to the total without throwing.
- [x] Break down "performance detail + discovery improvements" (ROADMAP "Next") into a first concrete slice: add a "More from this artist" or "Similar performances" section to `src/app/artist/[name]/page.tsx` showing up to 4 other performances by the same artist (excluding the one already in view, if the route ever scopes to a single performance) sourced from the same Supabase query already on the page — no new API calls, just a second rendered section using data already fetched. Acceptance: section hidden when the artist has ≤1 performance; covered by a test asserting the section renders the expected card count.
- [ ] Break down "notifications / reminders for saved performances" (ROADMAP "Later") into a first concrete slice: add a `reminder_opt_in` boolean column (via a new `supabase/*.sql` migration file, RLS-protected like `saved_performances`) and a toggle in `src/app/profile/page.tsx` next to each saved performance — no actual notification delivery yet (no email/push infra exists), just the data model + UI toggle so a later task can wire up delivery. Acceptance: toggle persists via Supabase, covered by a test mocking the update call; explicitly out of scope: any cron/email/push sending logic.
- [x] Break down "analytics on discovery funnel" (ROADMAP "Later") into a first concrete slice: add a minimal client-side event log (e.g. a `src/lib/analytics.ts` helper that no-ops today but defines a typed `track(event, props)` call) and call it from the three funnel entry points that already exist — search submit (`src/app/search/page.tsx`), artist card click (`ArtistCard`), and `SaveButton` toggle — without wiring up a real analytics backend yet (no new env vars, no third-party script). Acceptance: `track()` calls are typed and tested (assert they're called with expected event names/props); explicitly out of scope: choosing/integrating an actual analytics provider.
- [ ] Add error-boundary coverage for `src/app/artists/page.tsx` and `src/app/venues/page.tsx` matching the pattern already shipped for `/search` (#28) and `/artists`' Supabase-error path (#39) — verify `src/app/venues/page.tsx` has the same `{ data, error }` destructure + thrown-error-surfaces-as-banner behavior as `/artists` already does; if it's missing, add it; if present, add the missing test coverage proving it (mirroring `src/app/artists/page.test.tsx`'s error-path test).
- [ ] Add `layout.test.tsx` for `/artists` route — `src/app/artists/layout.tsx` exports `metadata` with `title: "Artists"` and `description` but has no test file; add `src/app/artists/layout.test.tsx` with 2 tests: assert `metadata.title` contains `"Artists"` and `metadata.description` is a non-empty string; follow the pattern from `src/app/landing/layout.test.tsx`.
- [ ] Add `layout.test.tsx` for `/venues` route — `src/app/venues/layout.tsx` exports `metadata` with `title: "Venues"` and `description` but has no test file; add `src/app/venues/layout.test.tsx` with 2 tests: assert `metadata.title` contains `"Venues"` and `metadata.description` is a non-empty string; follow the pattern from `src/app/landing/layout.test.tsx`.
- [ ] Add `layout.test.tsx` for `/search` route — `src/app/search/layout.tsx` exports `metadata` with `title: "Search performances"` and `description` but has no test; add `src/app/search/layout.test.tsx` with 2 tests asserting `metadata.title` and `metadata.description` are non-empty; follow the pattern from `src/app/landing/layout.test.tsx`.
- [ ] Add `layout.test.tsx` for `/profile` route — `src/app/profile/layout.tsx` exports `metadata` with `robots: { index: false, follow: false }` but has no test; add `src/app/profile/layout.test.tsx` asserting `metadata.robots.index === false` (confirming the profile is de-indexed) and `metadata.title` is non-empty.
- [ ] Add `page.test.tsx` for `src/app/login/page.tsx` — the login route file (`login/page.tsx`) wraps `<LoginForm />` in `<Suspense>` and exports `metadata`; `LoginForm.test.tsx` covers the form itself but not the page wrapper; add `src/app/login/page.test.tsx` that mocks `LoginForm` to a simple stub and asserts: the page renders without throwing; `metadata.title` is `"Sign in"`; the Suspense fallback is present in the component tree (use `React.Suspense`).
- [ ] Add `aria-busy={busy}` to `SaveButton` — `src/components/SaveButton.tsx` dims the button via `opacity: 0.6` when the save/unsave is in-flight (`busy` state) but emits no programmatic signal to assistive technology; add `aria-busy={busy}` to the `<button>` element and extend `src/components/SaveButton.test.tsx` with a test asserting `aria-busy="true"` is present while the `toggleSaved` promise is pending.
- [ ] Add `aria-busy={loadingMore}` to "Load more" button in `/search` — `src/app/search/page.tsx` line 288–293 renders a "Load more" button that changes text to "Loading…" when fetching but has no `aria-busy` attribute; add `aria-busy={loadingMore}` to the button and extend `page.test.tsx` with a test asserting `aria-busy="true"` appears when the next page is loading.
- [ ] Add `aria-pressed` to sort buttons on `/search` — `src/app/search/page.tsx` line 263–264 renders four sort buttons (Newest First, Oldest First, Artist A→Z, Most Sources) that are styled differently when active but have no `aria-pressed` attribute; add `aria-pressed={sort === val}` to each button and extend `page.test.tsx` to assert the active sort button has `aria-pressed="true"` and inactive ones have `aria-pressed="false"`.
- [ ] Add `alternates.canonical` to `/artists/layout.tsx` — the artist-detail page has a canonical URL (from `generateMetadata`), but the `/artists` listing page does not; add `alternates: { canonical: '/artists' }` to `src/app/artists/layout.tsx` and assert `metadata.alternates?.canonical` equals `'/artists'` in `layout.test.tsx` (create that file if needed).
- [ ] Add `alternates.canonical` to `/venues/layout.tsx` — same gap as `/artists`; add `alternates: { canonical: '/venues' }` to `src/app/venues/layout.tsx` and assert in `layout.test.tsx`.
- [ ] Add `alternates.canonical` to `/search/layout.tsx` — add `alternates: { canonical: '/search' }` to `src/app/search/layout.tsx` and assert in `layout.test.tsx`; canonical prevents duplicate-URL indexing when search params are included in crawled URLs.
- [ ] Add `role="menu"` / `role="menuitem"` semantics to PageNav account dropdown — the account dropdown in `src/components/PageNav.tsx` has focus-trap and Escape handling (PR #99) but the container div and its Profile/Sign-out items have no ARIA menu-role semantics; add `role="menu"` to the container div, `role="menuitem"` to the Profile link and Sign-out button, and cover in `PageNav.test.tsx` with assertions on the role attributes; this completes the ARIA widget pattern alongside `aria-expanded` (queued in groom) and focus-trap (#99).

## Done
<!-- routine PRs move completed items here -->
- [x] Add tests for `src/app/login/LoginForm.tsx` — mock `@/lib/supabase` and `next/navigation` (`useSearchParams`, `useRouter`); cover: (a) email + password sign-in success calls `router.push`; (b) Supabase sign-in error renders error banner; (c) submit button shows spinner when `busy=true`; (d) Google button calls `signInWithOAuth`; (e) toggling between sign-in / sign-up modes clears error and notice states; (f) `?error=auth` URL param pre-populates the error banner without triggering a Supabase call.
- [x] Add a test suite for `src/components/PageNav.tsx` — extended existing `PageNav.test.tsx` with auth-menu open/close, "Sign out" calls useSignOut, unauthenticated "Sign up free" → /login, and additional route-matching cases (/, /venues, /search); 11 tests total passing.
- [x] Add tests for `src/app/developer/page.tsx` — no test file exists (only `layout.test.tsx`); mock `@/lib/supabase`; cover: the "Performances" tab is active on load; switching tabs re-fetches and renders the relevant data table; the pipeline-run view renders columns; an inline Supabase error shows an error state rather than an empty table. (Shipped in this PR — `src/app/developer/page.test.tsx` added with 4 tests using a smart `makeBuilder` mock that routes count queries vs data queries to the appropriate response shape.)
- [x] Migrate `<img>` to `next/image` in `src/app/artists/page.tsx` — one raw `<img>` in `ArtistCard`; added `www.theaudiodb.com` to `next.config.ts` `remotePatterns`; replaced with `<Image fill>` keeping `objectFit: 'cover'` / `objectPosition: 'center top'` styles and `imgErr` fallback; `no-img-element` lint warning gone; existing 8 tests pass. (Shipped in this PR.)
- [x] Migrate `<img>` to `next/image` in `src/app/profile/page.tsx` — replaced three raw `<img>` tags (SavedCard thumbnail, TopPerfCard thumbnail, OAuth avatar) with `<Image>`; added `lh3.googleusercontent.com` to `next.config.ts` `remotePatterns`; used `fill`+`sizes` for thumbnails and `width={96}`/`height={96}` for avatar; `onError` fallback chains preserved; no `no-img-element` lint warnings for profile. (Shipped in this PR.)
- [x] Add tests for `src/app/profile/page.tsx` — 7 tests covering: (a) loading spinner while userLoading=true; (b) empty-saved "Browse Performances" CTA; (c) saved grid renders a card per entry; (d) remove button calls toggleSaved with correct ID; (e) Supabase error shows error state + retry; (f) stats tab renders platform breakdown; (g) optimistic remove rolls back on rejection. All 7 pass.
- [x] Migrate `<img>` to `next/image` in `src/app/artist/[name]/page.tsx` — replaced PerfCard thumbnail (`fill`) and artist avatar (`width={110} height={110}`); added `www.theaudiodb.com` to `next.config.ts` `remotePatterns`; fanart probe intentionally kept as raw `<img>`; typecheck clean.
- [x] Add a `<link rel="canonical">` to performance/artist detail pages — `src/app/artist/[name]/page.tsx` has `generateMetadata` (shipped in #48) but no explicit canonical URL; artist names with different casing/encoding could otherwise be indexed as duplicate pages.
- [x] Extract the duplicated admin-email-gating logic in `src/proxy.ts` and `src/app/developer/layout.tsx` into a shared `src/lib/isAdmin.ts` helper — both currently re-implement the `ADMIN_EMAILS` comparison independently (intentional defence-in-depth per `docs/ARCHITECTURE.md`, but the comparison logic itself — trim/case-handling — should live in one tested place so the two checks can't silently drift apart). (Shipped in this PR — added `src/lib/isAdmin.ts` exporting `isAdmin(email)`, which both `src/proxy.ts` and `src/app/developer/layout.tsx` now import and call instead of each defining a local `adminEmails()` function. Comparison semantics preserved exactly: comma-separated `ADMIN_EMAILS`, trimmed + lowercased, empty entries filtered. Added `src/lib/isAdmin.test.ts` covering case-insensitivity, whitespace trimming, comma-separated parsing, empty/unset `ADMIN_EMAILS`, non-admin email, and null/undefined/empty input. Existing `src/proxy.test.ts` and `src/app/developer/layout.test.tsx` pass unmodified.)
- [x] Add rate limiting to `/api/artists/search` — the route proxies to TheAudioDB with no caller-side throttling; a scripted client could hammer the endpoint and exhaust the (rate-limited) upstream API for all users; add a simple in-memory or edge-config token-bucket limiter keyed by IP. (In-memory token-bucket limiter keyed by caller IP via `x-forwarded-for`/`x-real-ip`, 10 requests per 10s window, in `src/lib/rateLimit.ts`; exceeding the limit returns HTTP 429 with `{ artists: [], error: 'rate_limited' }`.)
- [x] Add a `vitest` coverage threshold to CI — added `@vitest/coverage-v8@4.1.9` (matching installed `vitest@4.1.9`), a `coverage` block in `vitest.config.ts` (provider `v8`, `text`/`json-summary`/`html` reporters, `thresholds.lines: 70`), and switched `.github/workflows/test.yml`'s test step to a new `test:coverage` script (`vitest run --coverage`). Measured baseline line coverage is 85.23%; chose **70% lines** per the task's suggested baseline — comfortably below the current figure so trivial future changes won't break CI, while still catching a real regression (e.g. an untested new module dragging coverage down). Vitest fails the run by default when configured thresholds aren't met, so CI now goes red on a coverage drop below 70% lines.
- [x] Add `aria-current="page"` to the active link in `PageNav` — added `aria-current={activeTab === tab ? 'page' : undefined}` to the tab `<button>` in `src/components/PageNav.tsx`; covered by new `src/components/PageNav.test.tsx`.
- [x] Add a `404.tsx`/`not-found.tsx` for the root `src/app/` — added `src/app/not-found.tsx`, a Server Component (no `'use client'`) styled like `src/app/artist/[name]/not-found.tsx`/`error.tsx`, with a link back to `/landing`; covered by `src/app/not-found.test.tsx`.
- [x] Add unit tests for `savedStore` (`src/lib/savedStore.ts`). (Shipped in #26 — `src/lib/savedStore.test.ts` exists covering `ensureSavedLoaded` dedup/in-flight-promise reuse, `toggleSaved` optimistic-update-then-revert, and `resetSaved`. Queue entry was stale.)
- [x] Fix silent Supabase error in `src/app/search/page.tsx`. (Shipped in #28 — `searchError` state + inline banner confirmed present in `fetchPerformances`'s try/catch. Queue entry was stale.)
- [x] Add `global-error.tsx` for root layout errors. (Shipped in #30 — `src/app/global-error.tsx` exists as a Client Component with `<html>`/`<body>` and `unstable_retry`. Queue entry was stale.)
- [x] Harden `/auth/callback` error handling — handle missing/invalid `code`, surface a friendly error page (redirect to `/login?error=auth`) instead of throwing; add a test covering missing code, exchange failure, and success. (Shipped in #15/#24 — `src/app/auth/callback/route.ts` already redirects to `/login?error=auth` on missing/invalid code or exchange failure, and `route.test.ts` covers all three cases. Queue entry was stale.)
- [x] Add tests for the `useUser` auth store (`src/lib/useUser.ts`) — use `vi.mock` for `@/lib/supabase` and `next/navigation`; cover initial `getUser` load, `onAuthStateChange` updates, loading→resolved transition, and `useSignOut` redirect. (Shipped in #16 — `src/lib/useUser.test.ts` exists with the described coverage. Queue entry was stale.)
- [x] Fix infinite spinner in artist page on Supabase error — `src/app/artist/[name]/page.tsx` doesn't catch a thrown Supabase query; add try/catch so `loading` is set to false and the user sees an error message instead of a stuck spinner. (Shipped in #17 — try/catch + `loadError` state confirmed present. Queue entry was stale.)
- [x] Add tests for `SuggestFooter` (`src/components/SuggestFooter.tsx`) — mock Supabase, cover: submit button disabled when artist field is empty; successful insert shows thank-you state; Supabase error surfaces error text; "Suggest another" resets the form. (Shipped in #18 — `src/components/SuggestFooter.test.tsx` exists. Queue entry was stale.)
- [x] Add tests for `SaveButton` (`src/components/SaveButton.tsx`) — mock `useUser`, `useSavedIds`, `toggleSaved`; cover: unauthenticated click routes to `/login`; authenticated click calls `toggleSaved`; error shows tooltip; busy state blocks double-click. (Shipped in #20 — `src/components/SaveButton.test.tsx` exists. Queue entry was stale.)
- [x] Add `robots.ts` and `sitemap.ts` (App Router file conventions) — sitemap covering static routes and dynamic `/artist/[name]` pages fetched from Supabase; `noindex` on `/profile` and `/developer`. (Shipped in #21 — `src/app/robots.ts` disallows `/profile` and `/developer`; `src/app/sitemap.ts` includes dynamic artist entries; `/profile` additionally sets `robots: { index: false }` in its layout metadata. Queue entry was stale.)
- [x] Add `loading.tsx` skeletons for `/search`, `/artists`, `/venues`, and `/profile` — show a skeleton/spinner while Supabase data loads instead of a blank page. (Shipped in #22 — all four `loading.tsx` files exist. Queue entry was stale.)
- [x] Add `src/app/artist/[name]/loading.tsx` skeleton — a hero placeholder + grid of card skeletons that streams before the client component hydrates, replacing the spinner that currently only appears post-hydration. (Shipped in #23 — file exists. Queue entry was stale.)
- [x] Harden `SuggestFooter` submission (`src/components/SuggestFooter.tsx`) — validate non-empty artist name, max field lengths (artist ≤ 100 chars, event ≤ 150 chars, link ≤ 500 chars), and basic URL format for `link`; prevent duplicate rapid submissions with a `submitting` guard. (Shipped in #24 — `MAX_ARTIST`/`MAX_EVENT`/`MAX_LINK`, `isValidHttpUrl`, and the `submitting` guard are all present. Queue entry was stale.)
- [x] Cover admin gating in `src/proxy.ts` — tests proving non-`ADMIN_EMAILS` users are blocked from `/developer` and admins are allowed; mock `@supabase/ssr` and `next/server`.
- [x] Add CI workflow `.github/workflows/test.yml` — runs `npm ci`, typecheck, lint, build on PRs to main; green on the default branch.
- [x] Per-page metadata + OpenGraph — `generateMetadata` on key routes (home, profile, performance detail) with title/description/og tags.
- [x] Add tests for `src/app/developer/layout.tsx` — duplicates admin check from `proxy.ts` (defence in depth) but is untested; mock `@/lib/supabase/server` and `next/navigation`; cover: unauthenticated → redirect `/login?next=/developer`; non-admin → redirect `/landing`; admin → renders children.
- [x] Add AbortController cancellation to search fetch in `src/app/search/page.tsx`. (Shipped in #32 — `AbortController`/`AbortSignal` confirmed present in the fetch logic. Queue entry was stale.)
- [x] Add `aria-label` to `SuggestFooter` inputs. (Shipped in #33 — `aria-label`/`aria-required` confirmed present on all three fields. Queue entry was stale.)
- [x] Add input validation + tests for `/api/artists/search`. (Shipped in #34 — `q.trim()` length check, `AbortSignal.timeout(5000)`, and `route.test.ts` all confirmed present. Queue entry was stale.)
- [x] Add `Content-Security-Policy` headers in `next.config.ts`. (Shipped in #35 — `script-src`/`img-src`/`connect-src` confirmed present in `headers()`. Queue entry was stale.)
- [x] Validate required env vars at startup. (Shipped in #37 — `src/lib/env.ts` confirmed present and wired into `src/instrumentation.ts`. Queue entry was stale.)
- [x] Add ARIA labels to performance cards. (Shipped in #36 — `aria-label` with the `Watch {eventName} by {artistName}` pattern confirmed present in `src/app/search/page.tsx`. Queue entry was stale.)
- [x] Paginate search results. (Shipped in #38 — confirmed via `git log` on `main`. Queue entry was stale.)
- [x] Fix silent Supabase error in `/artists` page. (Shipped in #39 — `{ data, error }` destructure + `throw error` confirmed present in `load()`. Queue entry was stale.)
- [x] Add keyboard accessibility to `ArtistCard`/`VenueCard`. (Shipped in #40 — `role="link"`, `tabIndex={0}`, and `onKeyDown` confirmed present on both components. Queue entry was stale.)
- [x] Add tests for `/artists` page. (Shipped in #41 — confirmed via `git log` on `main`. Queue entry was stale.)
- [x] Add tests for `/venues` page. (Shipped in #43 — `src/app/venues/page.test.tsx` confirmed present. Queue entry was stale.)
- [x] Add tests for `src/app/sitemap.ts`. (Shipped in #44 — `src/app/sitemap.test.ts` confirmed present. Queue entry was stale.)
- [x] Add tests for `src/app/error.tsx`. (Shipped in #45 — `src/app/error.test.ts` confirmed present. Queue entry was stale.)
- [x] Migrate the performance thumbnail `<img>` in `src/app/search/page.tsx` to `next/image`. (Shipped in #46 — `import Image from 'next/image'` confirmed present, raw `<img>` removed. Queue entry was stale.)
- [x] Debounce the `/venues` and `/artists` page search inputs — `src/app/venues/page.tsx` and `src/app/artists/page.tsx` now keep an immediate `inputValue` bound to the `<input>` and only commit to the `filter` state (used by the `.filter()`/`.sort()` pipeline) after a 400ms debounce, mirroring the `debounceRef`/`setTimeout`/`clearTimeout`-on-unmount pattern from `src/app/search/page.tsx`. Added fake-timer tests to both `page.test.tsx` files covering "no filter before 400ms," "filters after 400ms," and "rapid typing resets the timer."
- [x] Add tests for `src/lib/supabase.ts` — the lazy Proxy-based singleton client has no coverage. (Shipped in #49 — `src/lib/supabase.test.ts` exists on main. Queue entry was stale.)
- [x] Disable the `X-Powered-By` response header. (Shipped in #50 — `poweredByHeader: false` confirmed present in `next.config.ts`. Queue entry was stale.)
- [x] Add a "Skip to main content" link and `<main>` landmark. (Shipped in #51 — skip link and `<main id="main">` confirmed present in `src/app/layout.tsx`. Queue entry was stale.)
- [x] Exclude `/api/` from `robots.ts`. (Shipped in #52 — `disallow: ["/profile", "/developer", "/api"]` confirmed present in `src/app/robots.ts`. Queue entry was stale.)
- [x] Add a `not-found.tsx` for `src/app/artist/[name]/`. (Shipped long ago in #11 — confirmed via `git log --all -S "notFound()" -- "src/app/artist/[name]/page.tsx"`: `src/app/artist/[name]/not-found.tsx` exists and `page.tsx` calls `notFound()` when `performances.length === 0`. This Queue entry was simply never reconciled until now, despite the underlying work shipping over a week before this grooming pass.)
- [x] Add an empty-state illustration/message to `/search` when a query returns zero results. (Already present since the very first scaffold commit (`4669c52`, "Add full public-facing frontend for LiveScore demo") — confirmed via `git log --all -S "No performances found for" -- src/app/search/page.tsx`. This Queue entry was stale from day one, predating even the task-queue/routine workflow itself.)
