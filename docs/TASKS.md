# LiveListen — Task Queue

This is the queue the **daily routine** reads. It takes the **top unblocked**
unchecked task, implements it on a `routine/<slug>` branch, and opens a PR.

Format: `- [ ] <title> — <acceptance criteria>`
- Top = highest priority. Add new work at the top to prioritize it.
- A task with a `> blocked: <reason>` line underneath is skipped until cleared.
- The routine ticks `[x]` inside its PR when done.

## Queue

- [ ] Fix silent Supabase error in `src/app/venues/page.tsx` — `load()` calls `supabase.from(...).select(...)` but only checks `if (!data)`, so a real Supabase error (RLS rejection, network failure) sets `data = null, error = <err>` and silently leaves the grid empty with no user feedback; add `{ data, error }` destructuring, a `loadError` state, and an error banner matching the `artists/page.tsx` pattern (`setLoadError(true)` when `error` is truthy); extend the existing `src/app/venues/page.test.tsx` with an error-path test case.
- [ ] Add `X-Content-Type-Options`, `X-Frame-Options`, and `Referrer-Policy` security headers in `next.config.ts` — the `headers()` block only returns `Content-Security-Policy`; add `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, and `Referrer-Policy: strict-origin-when-cross-origin` to the same `/(.*)`-source entry (one `headers` array, three new entries); no new deps required.
- [ ] Add `aria-label` to the filter inputs in `src/app/venues/page.tsx` and `src/app/artists/page.tsx` — both `<input>` elements have a `placeholder` but no `aria-label`, leaving unlabeled controls for screen-reader users; add `aria-label="Filter venues"` to the input in `venues/page.tsx` and `aria-label="Filter artists"` to the input in `artists/page.tsx`; add assertions to the existing `page.test.tsx` files that the labeled input is findable via `getByRole('textbox', { name: 'Filter venues' })`.
- [ ] Add `lastModified` to `src/app/sitemap.ts` entries and extend the sitemap test — all sitemap entries currently omit the `lastModified` field; add `lastModified: new Date()` to both static and dynamic entries (the `sitemap()` function runs server-side at request time, so `new Date()` reflects the request timestamp); extend `src/app/sitemap.test.ts` to assert `lastModified` is a valid `Date` instance (i.e. `expect(entry.lastModified).toBeInstanceOf(Date)`) for both static and dynamic entries.
- [ ] Migrate raw `<img>` to `next/image` in `src/app/artist/[name]/page.tsx` — 3 raw `<img>` tags remain (performance thumb at line 69, fanart background at line 187, artist info thumb at line 204); migrate each to `<Image>` with explicit `width`/`height` or `fill`; add `{ protocol: 'https', hostname: 'www.theaudiodb.com' }` to `next.config.ts` `images.remotePatterns` (it is already in the CSP `img-src` but not `remotePatterns`); lint warning count in `npm run lint` should drop by at least 3.
- [ ] Migrate raw `<img>` to `next/image` in `src/app/profile/page.tsx` — 3 raw `<img>` tags (performance thumbnails at lines 82 and 152, user avatar at line 305); migrate each to `<Image>` with `fill` or explicit dimensions; ensure `lh3.googleusercontent.com` is in `next.config.ts` `images.remotePatterns` (needed for Google OAuth avatar URLs).
- [ ] Migrate raw `<img>` to `next/image` in `src/app/artists/page.tsx` — 1 raw `<img>` at line 57 (artist thumbnail from TheAudioDB); replace with `<Image fill>` inside a `position: relative` container; ensure `www.theaudiodb.com` is in `images.remotePatterns` (see artist-page migration above).
- [ ] Migrate raw `<img>` to `next/image` in `src/components/PageNav.tsx` — 1 raw `<img>` at line 63 (signed-in user avatar from Google OAuth / `lh3.googleusercontent.com`); replace with `<Image width={32} height={32}>` (or whatever the current `width`/`height` style values are) and ensure `lh3.googleusercontent.com` is in `next.config.ts` `images.remotePatterns`.
- [ ] Add tests for `src/app/login/LoginForm.tsx` — no test file exists; mock `@/lib/supabase` and `next/navigation`; cover: empty email input keeps submit disabled; non-empty email enables submit; clicking submit calls `supabase.auth.signInWithOtp` with the typed email; Supabase error surfaces an error message in the UI; success hides the form and shows a "check your email" confirmation.
- [ ] Add tests for `src/app/profile/page.tsx` — no test file exists; mock `@/lib/useUser` and `@/lib/supabase`; cover: unauthenticated user is redirected to `/login`; loading state shows spinner; Supabase error renders an error banner; authenticated user sees their saved-performances stats and profile info rendered.
- [ ] Add tests for `src/app/landing/page.tsx` — no test file exists; cover: page renders without throwing; hero heading is visible; sign-in CTA link/button is present and has the correct href (e.g. `/login`).
- [ ] Add tests for `src/app/developer/page.tsx` — no test file exists; mock `@/lib/supabase/server`; cover: page renders the admin data table when Supabase returns rows; Supabase error is handled gracefully (error message rendered, no uncaught exception).
- [ ] Add a `<link rel="canonical">` to performance/artist detail pages — `src/app/artist/[name]/layout.tsx`'s `generateMetadata` (shipped in #48) has no explicit canonical URL; artist names with different casing/encoding could be indexed as duplicate pages; add `alternates: { canonical }` built from `NEXT_PUBLIC_SITE_URL + '/artist/' + encodeURIComponent(name)`, matching the convention `sitemap.ts` already uses.
  > open: PR #56 (`routine/artist-canonical-link`), awaiting review/merge.
- [ ] Add rate limiting to `/api/artists/search` — the route proxies to TheAudioDB with no caller-side throttling; a scripted client could hammer the endpoint and exhaust the (rate-limited) upstream API for all users; add a simple in-memory or edge-config token-bucket limiter keyed by IP.
  > open: PR #57 (`routine/rate-limit-artists-search`), awaiting review/merge.
- [ ] Add a `vitest` coverage threshold to CI — `.github/workflows/test.yml` runs `npm test` but doesn't fail the build if coverage regresses; add `--coverage` with a baseline threshold (e.g. 70% lines) so future PRs can't silently drop coverage on touched files.
  > open: PR #58 (`routine/vitest-coverage-threshold`), awaiting review/merge.
- [ ] Add `aria-current="page"` to the active link in `PageNav` — the nav highlights the current route visually (e.g. via active styling) but doesn't expose it to assistive tech; add `aria-current="page"` on the matching link so screen reader users know which section they're in.
  > open: PR #60 (`routine/pagenav-aria-current`), awaiting review/merge.
- [ ] Add a `404.tsx`/`not-found.tsx` for the root `src/app/` — there is currently no top-level `not-found.tsx`, so unmatched routes fall back to the Next.js default 404 page instead of the site's branded layout/nav; add one consistent with `global-error.tsx`'s pattern.
  > open: PR #61 (`routine/root-not-found`), awaiting review/merge.
- [ ] Add `Cache-Control` headers for static performance/artist data — `src/app/artist/[name]/page.tsx` and `src/app/search/page.tsx` refetch the same Supabase rows on every navigation with no caching layer; add Next.js `revalidate`/`fetch` cache hints (or `unstable_cache`) so repeat visits within a short TTL skip the round-trip.
  > blocked: both `artist/[name]/page.tsx` and `search/page.tsx` are `'use client'` components calling the browser Supabase singleton; `revalidate`/`unstable_cache` are server-only and cannot apply here. Requires migrating data fetching to server components first.
- [ ] Add a `robots.test.ts`-style test for `src/app/sitemap.ts`'s `lastModified` field — the existing sitemap test (shipped in #44) covers route presence/dedup but not date values; add a case asserting `lastModified` is a valid `Date` for both static and dynamic entries, guarding against a future refactor passing a malformed timestamp.
  > blocked: `lastModified` has never been added to `sitemap.ts` (verified on `main`); there is nothing to test. Superseded by the "Add `lastModified` to `sitemap.ts` entries" task above, which adds the field and the test together.

## Done
<!-- routine PRs move completed items here -->
- [x] Add tests for `src/lib/supabase.ts`. (Shipped in #49 — `src/lib/supabase.test.ts` confirmed present on `main`. Queue entry had a stale `> open: PR #49` note after merge; cleared in this grooming pass.)
- [x] Disable the `X-Powered-By` response header. (Shipped in #50 — `poweredByHeader: false` confirmed in `next.config.ts`. Stale `> open: PR #50` note cleared.)
- [x] Add a "Skip to main content" link and `<main>` landmark. (Shipped in #51 — skip link and `<main id="main">` confirmed in `src/app/layout.tsx`. Stale `> open: PR #51` note cleared.)
- [x] Exclude `/api/` from `robots.ts`. (Shipped in #52 — `/api` in `disallow` list confirmed in `src/app/robots.ts`. Stale `> open: PR #52` note cleared.)
- [x] Add a `not-found.tsx` for `src/app/artist/[name]/`. (Shipped in PR #11 — `src/app/artist/[name]/not-found.tsx` exists and `page.tsx` calls `notFound()` when Supabase returns no rows. Queue entry was stale from day one; never needed implementation.)
- [x] Add an empty-state illustration/message to `/search` when a query returns zero results. (Present since the original scaffold — `search/page.tsx` renders "No performances found for '{query}'" with a `SuggestFooter` pointer. Queue entry was stale from day one.)
- [x] Extract the duplicated admin-email-gating logic in `src/proxy.ts` and `src/app/developer/layout.tsx` into a shared `src/lib/isAdmin.ts` helper — both currently re-implement the `ADMIN_EMAILS` comparison independently (intentional defence-in-depth per `docs/ARCHITECTURE.md`, but the comparison logic itself — trim/case-handling — should live in one tested place so the two checks can't silently drift apart). (Shipped in this PR — added `src/lib/isAdmin.ts` exporting `isAdmin(email)`, which both `src/proxy.ts` and `src/app/developer/layout.tsx` now import and call instead of each defining a local `adminEmails()` function. Comparison semantics preserved exactly: comma-separated `ADMIN_EMAILS`, trimmed + lowercased, empty entries filtered. Added `src/lib/isAdmin.test.ts` covering case-insensitivity, whitespace trimming, comma-separated parsing, empty/unset `ADMIN_EMAILS`, non-admin email, and null/undefined/empty input. Existing `src/proxy.test.ts` and `src/app/developer/layout.test.tsx` pass unmodified.)
- [x] Add unit tests for `savedStore` (`src/lib/savedStore.ts`). (Shipped in #26 — `src/lib/savedStore.test.ts` exists covering `ensureSavedLoaded` dedup/in-flight-promise reuse, `toggleSaved` optimistic-update-then-revert, and `resetSaved`. Queue entry was stale.)
- [x] Fix silent Supabase error in `src/app/search/page.tsx`. (Shipped in #28 — `searchError` state + inline banner confirmed present in `fetchPerformances`'s try/catch. Queue entry was stale.)
- [x] Add `global-error.tsx` for root layout errors. (Shipped in #30 — `src/app/global-error.tsx` exists as a Client Component with `<html>`/`<body>` and `unstable_retry`. Queue entry was stale.)
- [x] Harden `/auth/callback` error handling — handle missing/invalid `code`, surface a friendly error page (redirect to `/login?error=auth`) instead of throwing; add a test covering missing code, exchange failure, and success. (Shipped in #15/#24 — `src/app/auth/callback/route.ts` already redirects to `/login?error=auth` on missing/invalid code or exchange failure, and `route.test.ts` covers all three cases. Queue entry was stale.)
- [x] Add tests for the `useUser` auth store (`src/lib/useUser.ts`) — use `vi.mock` for `@/lib/supabase` and `next/navigation`; cover initial `getUser` load, `onAuthStateChange` updates, loading→resolved transition, and `useSignOut` redirect. (Shipped in #16 — `src/lib/useUser.test.ts` exists with the described coverage. Queue entry was stale.)
- [x] Fix infinite spinner in artist page on Supabase error — `src/app/artist/[name]/page.tsx` doesn't catch a thrown Supabase query; add try/catch so `loading` is set to false and the user sees an error message instead of a stuck spinner. (Shipped in #17 — try/catch + `loadError` state confirmed present. Queue entry was stale.)
- [x] Add tests for `SuggestFooter` (`src/components/SuggestFooter.tsx`) — mock Supabase, cover: submit button disabled when artist field is empty; successful insert shows thank-you state; Supabase error surfaces error text; "Suggest another" resets the form. (Shipped in #18 — `src/components/SuggestFooter.test.tsx` exists. Queue entry was stale.)
- [x] Add tests for `SaveButton` (`src/components/SaveButton.tsx`) — mock `useUser`, `useSavedIds`, `toggleSaved`; cover: unauthenticated click routes to `/login`; authenticated click calls `toggleSaved`; error shows tooltip; busy state blocks double-click. (Shipped in #20 — `src/components/SaveButton.test.tsx` exists. Queue entry was stale.)
- [x] Add `robots.ts` and `sitemap.ts` (App Router file conventions) — sitemap covering static routes and dynamic `/artist/[name]` pages fetched from Supabase; `noindex` on `/profile` and `/developer`. (Shipped in #21 — `src/app/robots.ts` disallows `/profile` and `/developer`; `src/app/sitemap.ts` includes dynamic artist entries; `/profile` additionally sets `robots: { index: false }` in its layout metadata. Queue entry was stale.)
- [x] Add `loading.tsx` skeletons for `/search`, `/artists`, `/venues`, and `/profile` — show a skeleton/spinner while Supabase data loads instead of a blank page. (Shipped in #22 — all four `loading.tsx` files exist. Queue entry was stale.)
- [x] Add `src/app/artist/[name]/loading.tsx` skeleton — a hero placeholder + grid of card skeletons that streams before the client component hydrates, replacing the spinner that currently only appears post-hydration. (Shipped in #23 — file exists. Queue entry was stale.)
- [x] Harden `SuggestFooter` submission (`src/components/SuggestFooter.tsx`) — validate non-empty artist name, max field lengths (artist ≤ 100 chars, event ≤ 150 chars, link ≤ 500 chars), and basic URL format for `link`; prevent duplicate rapid submissions with a `submitting` guard. (Shipped in #24 — `MAX_ARTIST`/`MAX_EVENT`/`MAX_LINK`, `isValidHttpUrl`, and the `submitting` guard are all present. Queue entry was stale.)
- [x] Cover admin gating in `src/proxy.ts` — tests proving non-`ADMIN_EMAILS` users are blocked from `/developer` and admins are allowed; mock `@supabase/ssr` and `next/server`.
- [x] Add CI workflow `.github/workflows/test.yml` — runs `npm ci`, typecheck, lint, build on PRs to main; green on the default branch.
- [x] Per-page metadata + OpenGraph — `generateMetadata` on key routes (home, profile, performance detail) with title/description/og tags.
- [x] Add tests for `src/app/developer/layout.tsx` — duplicates admin check from `proxy.ts` (defence in depth) but is untested; mock `@/lib/supabase/server` and `next/navigation`; cover: unauthenticated → redirect `/login?next=/developer`; non-admin → redirect `/landing`; admin → renders children.
- [x] Add AbortController cancellation to search fetch in `src/app/search/page.tsx`. (Shipped in #32 — `AbortController`/`AbortSignal` confirmed present in the fetch logic. Queue entry was stale.)
- [x] Add `aria-label` to `SuggestFooter` inputs. (Shipped in #33 — `aria-label`/`aria-required` confirmed present on all three fields. Queue entry was stale.)
- [x] Add input validation + tests for `/api/artists/search`. (Shipped in #34 — `q.trim()` length check, `AbortSignal.timeout(5000)`, and `route.test.ts` all confirmed present. Queue entry was stale.)
- [x] Add `Content-Security-Policy` headers in `next.config.ts`. (Shipped in #35 — `script-src`/`img-src`/`connect-src` confirmed present in `headers()`. Queue entry was stale.)
- [x] Validate required env vars at startup. (Shipped in #37 — `src/lib/env.ts` confirmed present and wired into `src/instrumentation.ts`. Queue entry was stale.)
- [x] Add ARIA labels to performance cards. (Shipped in #36 — `aria-label` with the `Watch {eventName} by {artistName}` pattern confirmed present in `src/app/search/page.tsx`. Queue entry was stale.)
- [x] Paginate search results. (Shipped in #38 — confirmed via `git log` on `main`. Queue entry was stale.)
- [x] Fix silent Supabase error in `/artists` page. (Shipped in #39 — `{ data, error }` destructure + `throw error` confirmed present in `load()`. Queue entry was stale.)
- [x] Add keyboard accessibility to `ArtistCard`/`VenueCard`. (Shipped in #40 — `role="link"`, `tabIndex={0}`, and `onKeyDown` confirmed present on both components. Queue entry was stale.)
- [x] Add tests for `/artists` page. (Shipped in #41 — confirmed via `git log` on `main`. Queue entry was stale.)
- [x] Add tests for `/venues` page. (Shipped in #43 — `src/app/venues/page.test.tsx` confirmed present. Queue entry was stale.)
- [x] Add tests for `src/app/sitemap.ts`. (Shipped in #44 — `src/app/sitemap.test.ts` confirmed present. Queue entry was stale.)
- [x] Add tests for `src/app/error.tsx`. (Shipped in #45 — `src/app/error.test.ts` confirmed present. Queue entry was stale.)
- [x] Migrate the performance thumbnail `<img>` in `src/app/search/page.tsx` to `next/image`. (Shipped in #46 — `import Image from 'next/image'` confirmed present, raw `<img>` removed. Queue entry was stale.)
- [x] Debounce the `/venues` and `/artists` page search inputs — `src/app/venues/page.tsx` and `src/app/artists/page.tsx` now keep an immediate `inputValue` bound to the `<input>` and only commit to the `filter` state (used by the `.filter()`/`.sort()` pipeline) after a 400ms debounce, mirroring the `debounceRef`/`setTimeout`/`clearTimeout`-on-unmount pattern from `src/app/search/page.tsx`. Added fake-timer tests to both `page.test.tsx` files covering "no filter before 400ms," "filters after 400ms," and "rapid typing resets the timer."
